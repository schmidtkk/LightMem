interface:
  display_name: LightMem Index
  short_description: Regenerate the LightMem topic index from topic files.
  default_prompt: Use LightMem index for this task.
policy:
  allow_implicit_invocation: true
